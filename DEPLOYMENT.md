# 🚀 Инструкция по деплою на PythonAnywhere

## Проблема: Footer не отображается на PythonAnywhere

**Причина:** Статические файлы (CSS) не обновились после изменений.

## Решение (пошагово):

### 1. На локальной машине:

```bash
# Закоммитить и запушить изменения
git add .
git commit -m "Update styles: optimize cards and add footer"
git push origin main
```

### 2. На PythonAnywhere (в Bash консоли):

```bash
# Перейти в папку проекта
cd ~/qr_code_menu

# Подтянуть изменения из GitHub
git pull origin main

# Очистить старые статические файлы и собрать новые
python manage.py collectstatic --noinput --clear

# Перезапустить приложение (замените USERNAME на ваше имя пользователя)
touch /var/www/USERNAME_pythonanywhere_com_wsgi.py
```

### 3. В браузере:

- Очистить кэш браузера: **Ctrl+Shift+R** (или **Cmd+Shift+R** на Mac)
- Обновить страницу

## Быстрый деплой (автоматический скрипт):

```bash
# На локальной машине
bash deploy_to_pythonanywhere.sh
```

Скрипт покажет команды, которые нужно выполнить на PythonAnywhere.

## Проверка настроек статических файлов

На PythonAnywhere в разделе **Web**:

- **Static files URL:** `/static/`
- **Static files directory:** `/home/USERNAME/qr_code_menu/static/`

## Проверка режима DEBUG

В `config/settings.py` на PythonAnywhere должно быть:
```python
DEBUG = False
```

## Частые ошибки:

### ❌ Footer не появляется
- Забыли выполнить `collectstatic`
- Не очистили кэш браузера
- Неправильный путь к статическим файлам

### ❌ Кнопка редактирования не видна
- Забыли добавить `?edit=BuXoRo7410` в URL
- Используете `/` вместо `/v2/`
- Правильный URL: `https://yoursite.pythonanywhere.com/v2/?edit=BuXoRo7410`

### ❌ Изображения не загружаются
- Проверьте путь к MEDIA_ROOT в настройках Web на PythonAnywhere
- **Media files URL:** `/media/`
- **Media files directory:** `/home/USERNAME/qr_code_menu/media/`

## Полезные команды

```bash
# Проверить статус Git
git status

# Посмотреть логи на PythonAnywhere
tail -f /var/log/USERNAME.pythonanywhere.com.error.log

# Перезапустить приложение
touch /var/www/USERNAME_pythonanywhere_com_wsgi.py
```

## Важно! 🔐

Не забудьте добавить `.env` файл на PythonAnywhere с теми же переменными:

```bash
SECRET_KEY=ваш-секретный-ключ
DEBUG=False
TELEGRAM_TOKEN=ваш-токен
EDIT_SECRET_KEY=BuXoRo7410
MEDIA_ROOT=/home/USERNAME/qr_code_menu/media
```